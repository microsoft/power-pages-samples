# ⚠️ SECURITY: do NOT commit a real secret. For production, store the client
# secret in Azure Key Vault and reference it through an environment variable, then
# point this site setting at that environment variable (see README). The
# placeholder below is intentionally empty.
description: Client secret of the Entra app registration (Graph client-credentials). Store in Key Vault / an environment variable for production — never a real value in source.
id: 3cb3b80d-ae2c-4a28-800d-07c95f5ff20b
name: SharePoint/ClientSecret
value: ''
