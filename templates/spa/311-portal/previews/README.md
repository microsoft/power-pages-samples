# Preview images

This folder contains PNG previews for the 311 Portal template.

These images were captured from the supplied local site build at `~/Downloads/templates-work/311-portal/Site/Zava 311 wo Node`.
The service catalog and service detail pages were skipped because they need Dataverse data at runtime.
